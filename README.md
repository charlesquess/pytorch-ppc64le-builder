# pytorch-ppc64le-builder
A one-click build script for PyTorch 2.5.1 on IBM PowerPC (ppc64le). Solves compilation issues with GCC 12 and CUDA on NVIDIA V100/A100
适用于 IBM PowerPC (ppc64le) 架构的 PyTorch 2.5.1 一键编译脚本。修复 GCC 12 + CUDA 环境下的各种编译报错，在 Tesla V100 上验证通过。
